// const solarSystemContainer = document.getElementById('solar-system-container');
// const planets = document.querySelectorAll('.celestial-body-shared');

// let solarSytemTransform = 0;
// let planetTransform = 0;

// document.addEventListener('keydown', (event) => {
//     if (event.key === 'ArrowDown') {
//         solarSystemContainer.style.transform = `rotateX(${solarSytemTransform = solarSytemTransform + 5}deg)`;
//         planets.forEach((planet) => {
//             planet.style.transform = `rotateX(${planetTransform = planetTransform - 5}deg)`;
//         })
//     }
// })

// up arrow is ArrowUp
